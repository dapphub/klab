#!/bin/sh
echo "Commands:
klab build [options] <spec>
klab run [options] <spec>

klab build options:
  --filter=<filter>

klab run options:
  --force               No replay
  --inspect=<id>
  --clean
  --name=<name>
  --headless
"
