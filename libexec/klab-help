#!/bin/sh
echo "Commands:
klab build [options]
klab run [options]

klab build options:
  --spec=<spec>
  --filter=<filter>

klab run options:
  --force               No replay
  --inspect=<id>
  --clean
  --spec=<spec>
  --name=<name>
  --headless
"
