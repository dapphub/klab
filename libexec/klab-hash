#!/usr/bin/env bash

name=$1

if [ -z "$KLAB_OUT" ]; then
  KLAB_OUT=out
fi

if [ -f "$KLAB_OUT/meta/name/$name" ]; then
  hash=$(cat "$KLAB_OUT/meta/name/$name")
else
  echo "ERROR: $name is unknown"
  exit 1;
fi;

echo "$hash";
